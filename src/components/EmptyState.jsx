export default function EmptyState({ message }) {
  return (
    <div
      style={{
        marginTop: "40px",
        textAlign: "center",
        color: "#9ca3af",
        fontSize: "15px",
      }}
    >
      {message}
    </div>
  );
}
